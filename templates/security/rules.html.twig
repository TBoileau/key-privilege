{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ rules.content|raw }}
        {{ rules.publishedAt|date("d/m/Y") }}
        {{ form_errors(form) }}
    {{ form_end(form) }}
{% endblock %}
