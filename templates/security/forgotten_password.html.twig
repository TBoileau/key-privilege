{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_errors(form) }}
        <button>
            Valider
        </button>
    {{ form_end(form) }}
{% endblock %}
