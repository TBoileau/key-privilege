{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_errors(form) }}
        <button>
            Supprimer
        </button>
    {{ form_end(form) }}
{% endblock %}
